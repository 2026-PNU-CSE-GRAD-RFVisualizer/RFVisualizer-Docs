# 3DGS 기반 공간 데이터 시각화 과제 - 임베디드 파트 중간보고서

- 보고 기준일: 2026-07-14
- 담당자: 이나영
- 담당 영역: 임베디드
- 대상 범위: 고정 RSSI 측정 노드, ESP32 게이트웨이, STM32 수신부, MQTT 연계

## 1. 현재 진척도 요약

현재 임베디드 파트는 **고정 RSSI 노드의 소프트웨어 프로토타입과 서버 연결을 완료하고, 멀티 노드 실물 시험과 대시보드·그래픽스 활용 검증을 진행하는 단계**이다.

- 전체 임베디드 계획(MVP-A~C) 대비 진척도: **약 30~35%**
- 현재 최우선 범위인 고정 RSSI 수집 MVP-A 대비 진척도: **약 65~70%**
- 단계 판단: **2단계 최소 계측 기능의 소프트웨어 구현 완료, 3단계 필터링·멀티 노드 확장 진입**
- 현재까지 확보한 핵심 결과:
  - ESP32 RSSI 노드 펌웨어 구현 및 빌드 성공
  - ESP32 게이트웨이 펌웨어 구현 및 빌드 성공
  - ESP-NOW → UART → STM32 수신 경로 구현
  - STM32 UART 인터럽트 수신, 체크섬 검증, 노드 테이블 갱신, JSON 생성 구현
  - STM32 펌웨어 빌드 및 J-Link 플래시 성공 이력 확보
  - STM32 파서 호스트 테스트 통과
  - STM32 시리얼 JSON을 MQTT로 전달하는 PC 브리지 구현
  - 노드별 고정 좌표를 MQTT 데이터에 결합하는 설정 파일 구현
  - MQTT Broker 및 서버 연결, 데이터 Publish 정상 동작 확인
- 현재 핵심 미완료 항목:
  - ESP32 3개 이상의 실물 동시 동작 및 장시간 계측 시험
  - 현재 동작 중인 MQTT Topic/JSON Schema의 최종 문서화 및 팀 공통 규격 확정
  - 대시보드 표시와 그래픽스 활용까지 포함한 최종 End-to-End 검증
  - Moving Average와 Median Filter의 정량 비교
  - 자동 재연결, RAM Ring Buffer, Watchdog 및 2시간 안정성 시험
  - 핸드헬드 IMU·버튼·LCD·JPEG 스트리밍 기능

진척도는 파일 또는 빌드 결과가 존재하는 것만으로 완료 처리하지 않고, 실물 시험과 백엔드 확인까지 끝난 경우를 최종 완료로 판단하여 산정하였다.

---

## 2. 요구조건 및 제약 사항 분석에 대한 수정사항

### 2.1 최초 요구조건

착수보고서에서 임베디드 파트의 주요 요구조건은 다음과 같이 정의되었다.

1. 여러 ESP32가 동시에 RSSI를 측정해야 한다.
2. 측정, 통신, 상태 관리 태스크가 서로 간섭하지 않아야 한다.
3. 각 노드는 노드 ID, Timestamp, RSSI 및 상태 정보를 포함한 표준 메시지를 전송해야 한다.
4. Wi-Fi 단절, 서버 지연 및 일시적 전원 불안정 상황에서도 데이터 손실을 최소화해야 한다.
5. 노드 증가 시에도 구조 변경을 최소화할 수 있어야 한다.
6. 현장에서 RSSI와 노드·통신 상태를 확인할 수 있어야 한다.
7. 최종적으로 수집 데이터를 3DGS 기반 RF Field 및 3차원 히트맵과 연계해야 한다.

### 2.2 중간 구현 과정에서 수정·구체화한 내용

| 구분 | 초기 계획 | 현재 수정·구체화 내용 | 수정 이유 |
|---|---|---|---|
| 고정 노드 전송 구조 | 각 ESP32가 Wi-Fi/MQTT로 직접 전송 | 1차 프로토타입은 원격 ESP32 노드 → ESP-NOW → ESP32 게이트웨이 → UART → STM32 → PC MQTT 브리지 구조 적용 | 다수 노드의 Wi-Fi/MQTT 동시 처리 부담을 줄이고, STM32 수신·전처리 기능을 독립 검증하기 위함 |
| 노드 구성 | 고정 ESP32 3개 이상 | ESP32 5개 기준으로 원격 노드 1~4와 게이트웨이 겸 로컬 노드 5로 구체화 | 보드 수를 효율적으로 사용하면서 5개 위치의 RSSI를 수집하기 위함 |
| RSSI 대상 식별 | 고정 BSSID 우선 | 현재 Bring-up 설정은 `U1MU2_2F`, 채널 6, SSID 기준 검색 사용 | 실제 AP 식별과 ESP-NOW 채널 고정을 우선 검증하기 위함. 최종 현장 시험 전 BSSID 고정 필요 |
| RSSI 전처리 | Moving Average 또는 Median Filter | 현재 5개 샘플 Moving Average 구현 | 계산량이 작고 1차 안정화에 적합함. Median Filter는 비교 시험 후 채택 여부 결정 |
| 무선 채널 | 별도 제약 미정 | 목표 AP 채널과 ESP-NOW 채널을 동일한 채널 6으로 고정 | ESP-NOW와 Wi-Fi Scan의 채널 불일치 방지 |
| STM32 역할 | 보드 확정 전 이식 가능한 파서 우선 | STM32F107VCT6에서 USART1 115200 bps 수신, 파싱, 노드 상태 관리 및 JSON 출력까지 구현 | 실제 보드에서 수신 체인을 검증하고 이후 네트워크 모듈 변경에 대응하기 위함 |
| MQTT 전송 주체 | ESP32 고정 노드 직접 Publish | 현재 STM32는 MQTT-ready JSON을 만들고, PC Python 브리지가 MQTT Publish 수행 | STM32 네트워크 인터페이스가 확정되지 않은 상태에서 전체 데이터 경로를 먼저 검증하기 위함 |
| MQTT 데이터 규격 | `rfmap/nodes/{node_id}/measurement`, `rfmap/nodes/{node_id}/status` 권장 | 현재 실행 코드는 `rssi/{node_id}`, `gateway/{gateway_id}`, `status/{gateway_id}/lwt`를 사용하며 서버 연결과 Publish가 정상 동작함 | 현재 동작 규격을 기준으로 팀 공통 문서를 갱신하거나 최종 Topic을 동결해야 함 |
| 좌표 정보 | `coord_frame_id`와 노드 좌표 포함 | `node_positions.json`을 통해 `pos_x`, `pos_y`, `pos_z` 결합 구현 | 펌웨어와 설치 위치 설정을 분리하여 현장 배치 변경에 대응하기 위함 |
| 핸드헬드 위치 추정 | 연속 위치 추적 가능성 검토 | 연속 6DoF 위치 추적을 제외하고 버튼 기반 Position Snap을 우선 | IMU 적분 오차와 RSSI 거리 추정 오차로 인한 범위 확대 방지 |
| LCD 시연 | 휴대용 장치에서 상태 및 3D 결과 확인 | LCD·JPEG 스트리밍은 고위험 MVP-C로 분리하고 실패 시 PC Viewer를 사용 | 480×800 LCD 병렬 인터페이스, PSRAM 및 디코딩 성능 위험을 핵심 RSSI 수집과 분리하기 위함 |

### 2.3 현재 확인된 제약 및 대응 방향

1. **ESP-NOW와 Wi-Fi Scan의 채널 제약**
   - 현재 목표 AP와 ESP-NOW를 모두 채널 6으로 고정하였다.
   - 현장 AP 채널이 변경되면 모든 노드와 게이트웨이 설정을 함께 변경해야 한다.

2. **현재 Gateway MAC 주소가 Broadcast 값으로 설정됨**
   - 노드 설정의 `GATEWAY_ESPNOW_MAC`은 현재 `FF:FF:FF:FF:FF:FF`이다.
   - 실물 멀티 노드 시험 전 실제 게이트웨이 MAC을 확인하여 Unicast 전송으로 변경하고 송신 성공률을 비교해야 한다.

3. **SSID 기준 측정의 모호성**
   - 같은 SSID를 사용하는 AP가 여러 개이면 측정 대상이 달라질 수 있다.
   - RF Field 실험 전 대상 AP의 BSSID를 확정하고 `TARGET_AP_USE_BSSID`를 활성화해야 한다.

4. **MQTT 규격 문서와 실행 코드의 차이**
   - 현재 MQTT Broker 및 서버 연결과 데이터 Publish는 정상 동작한다.
   - 다만 최종 계획서의 권장 Topic과 현재 실행 코드의 Topic이 다르므로, 동작 중인 규격을 공통 문서에 반영하고 대시보드·그래픽스 소비 규격까지 동결해야 한다.

5. **STM32 네트워크 경로 미확정**
   - 현재 STM32는 UART 수신 및 JSON 출력까지 담당하며 MQTT 전송은 PC 브리지가 수행한다.
   - 최종 하드웨어에서 Ethernet, Wi-Fi 모듈 또는 PC 브리지 중 어떤 경로를 사용할지 팀 합의가 필요하다.

6. **RSSI의 환경 민감성**
   - RSSI는 동일 위치에서도 인체, 문, 장애물 및 간섭에 따라 변동한다.
   - RSSI를 직접 거리로 환산하는 방식보다 Fingerprinting 또는 구역 Snap을 우선한다.

7. **장시간 신뢰성 미검증**
   - 현재 빌드와 단위 파서 시험은 통과했지만 1시간·2시간 실물 연속 시험은 완료되지 않았다.
   - 안정성 시험 완료 전에는 최종 완료로 판단하지 않는다.

---

## 3. 설계 상세화 및 변경 내역

### 3.1 현재 시스템 구조

```text
[ESP32 원격 RSSI 노드 1~4]
  - 대상 AP RSSI Scan
  - 5개 샘플 Moving Average
  - node_id / seq / uptime / raw RSSI / filtered RSSI / error_flags
  - CRC32가 포함된 ESP-NOW 패킷
              │
              └──────── ESP-NOW ────────┐
                                         ▼
[ESP32 Gateway + Local Node 5]
  - 원격 노드 패킷 수신 및 CRC 검증
  - 노드별 최신 Sequence, 중복 수, 유실 수 관리
  - 게이트웨이 자체 RSSI를 node_id 5로 측정
  - XOR Checksum이 포함된 UART Line 생성
                                         │ 115200 bps UART
                                         ▼
[STM32F107VCT6]
  - USART1 RX 인터럽트 기반 Byte Stream 수신
  - UART Line 재동기화 및 Checksum 검증
  - 노드별 최신값·Timeout·Sequence 상태 관리
  - MQTT-ready JSON Snapshot 생성 및 UART 출력
                                         │ USB Serial / COM4
                                         ▼
[Python Serial-MQTT Bridge]
  - STM32 JSON 정규화
  - 노드 ID 표준 문자열 변환
  - 노드별 좌표 결합
  - MQTT Publish
                                         │
                                         ▼
[MQTT Broker → Backend → Dashboard / Graphics]
```

### 3.2 모듈별 상세 구현 상태

| 모듈 | 주요 구현 내용 | 상태 |
|---|---|---|
| ESP32 RSSI Node | RSSI 측정 Task, ESP-NOW 송신 Task, Health Task 분리 | 구현 완료, 실물 다중 시험 필요 |
| RSSI Filter | 유효 범위 검사, 5개 샘플 이동평균, x10 고정소수점 변환 | 구현 완료, Median 비교 필요 |
| ESP-NOW Packet | Magic, Version, Node ID, Sequence, Uptime, BSSID, RSSI, Error Flags, CRC32 | 구현 완료 |
| ESP32 Gateway | ESP-NOW 수신, CRC 검사, 노드 테이블, 중복·누락 집계 | 구현 완료 |
| Gateway Local Node | 게이트웨이를 node_id 5로 사용하여 로컬 RSSI 측정 | 구현 완료 |
| UART Protocol | `$RSSI,...*XX` 형식, XOR Checksum, 115200 bps | 구현 완료 |
| STM32 Parser | Byte 단위 수신, Checksum/Format 오류 구분, 라인 재동기화 | 구현 완료 및 호스트 시험 통과 |
| STM32 Preprocessor | 노드별 최신값, Sequence 손실, Timeout 상태 관리 | 구현 완료 |
| STM32 JSON | Gateway ID, Timestamp, 노드별 RSSI·상태를 포함한 Snapshot 생성 | 구현 완료 |
| MQTT Bridge | 시리얼 JSON 수신, 과거/현재 JSON 호환, 위치 결합, MQTT Publish | 구현 완료, 서버 연결 및 Publish 정상 |
| 좌표 설정 | node-01~04, gw-01의 좌표 설정 파일 분리 | 구조 완료, 실제 좌표 입력 필요 |
| 서버·백엔드 | MQTT Broker 및 서버 연결, 데이터 수신 | 연결 정상, 현재 데이터 경로 동작 |
| 대시보드·그래픽스 | 수신 데이터 표시 및 활용 | 최종 활용 검증 필요 |
| 장애 대응 | 자동 재연결, Ring Buffer, Watchdog, Reset Reason | 미완료 |
| 핸드헬드 | IMU, 버튼, UDP Pose, LCD, JPEG 수신·디코딩 | 미착수 |

### 3.3 주요 변경 이력

| 시점 | 변경 내용 | 결과 |
|---|---|---|
| 2026-06-25 | 임베디드 구현 범위를 고정 RSSI 노드, 핸드헬드, 고위험 LCD 시연으로 분리 | 핵심 MVP와 확장 범위 구분 |
| 2026-06-26 | ESP32 다중 노드 → ESP-NOW Gateway → STM32 프로토타입 작업 정의 | 초기 하드웨어·통신 구조 구체화 |
| 2026-06-30 전후 | ESP32 Node/Gateway 펌웨어 및 프로토콜 모듈 구현 | ESP32 빌드 산출물 생성 |
| 2026-07-03 | STM32F107VCT6 CMake/Ninja 빌드 및 J-Link 플래시 | ELF/HEX/BIN 생성, Under-reset 방식으로 실행 확인 |
| 2026-07-10 전후 | ESP32 Node/Gateway 재빌드, STM32 수신부 통합, 시리얼-MQTT 브리지 및 좌표 결합 보완 | 현재 소프트웨어 체인 구성 완료 |
| 2026-07-14 | 전체 모듈 재검증 | STM32·ESP32 빌드 성공, 파서 시험 및 Python 문법 검사 통과 |

---

## 4. 갱신된 과제 추진 계획

현재 일정은 고정 RSSI 수집 파이프라인의 종단 검증을 우선하고, 핸드헬드 기능은 MVP-A 안정화 이후 진행하도록 갱신한다.

### 4.1 단기 계획: 2026-07-14 ~ 2026-07-22

| 우선순위 | 작업 | 완료 기준 | 산출물 |
|---|---|---|---|
| P0 | 현재 동작 중인 MQTT Topic 및 JSON Schema 문서화·동결 | 서버가 정상 수신 중인 Topic/Payload를 팀 공통 계약으로 확정 | Schema 문서와 수신 로그 |
| P0 | 1개 노드 End-to-End 최종 검증 | RSSI 측정 → STM32 → MQTT → Server → Dashboard 표시 | 화면 캡처 및 로그 |
| P0 | 실제 AP BSSID와 Gateway MAC 확정 | 모든 장치가 동일 AP·채널·Gateway를 사용 | 장치별 설정표 |
| P1 | ESP32 노드 3개 이상 실물 동시 시험 | 고유 Node ID 유지, 1 Hz 전송, UART 라인 손상 없음 | 30분 이상 시험 로그 |
| P1 | Moving Average와 Median Filter 비교 | 동일 위치 데이터의 평균·표준편차·최대 편차 비교 | CSV와 그래프 |
| P1 | 노드 실제 좌표 입력 | 3DGS 좌표계와 일치하는 위치 데이터 전송 | 갱신된 좌표 파일 |
| P1 | Time Window 동기화 협의 | 백엔드에서 동일 시간 구간의 멀티 노드 프레임 생성 | 인터페이스 합의서 |

### 4.2 안정화 계획: 2026-07-23 ~ 2026-07-31

1. AP 및 MQTT Broker 중단 후 자동 재연결 구현
2. 제한 크기 RAM Ring Buffer와 오래된 데이터 Drop 정책 구현
3. 재전송 횟수, Drop Count, Reconnect Count, Free Heap 및 Reset Reason 전송
4. Watchdog 적용 및 비정상 Task 정지 시험
5. 1시간 단위 시험 후 2시간 이상 연속 안정성 시험
6. 패킷 손실률, 지연시간, 재연결 시간 및 RSSI 변동 통계 작성

### 4.3 핸드헬드 계획: 2026-08-01 이후

1. 스마트 IMU 모델과 핀맵 확정
2. Quaternion/Rotation Vector 수신 및 UDP Pose 전송
3. Recenter 및 Position Update 버튼 구현
4. PC Viewer에서 Orientation과 Position Snap 연동
5. LCD 단색·테스트 이미지 출력
6. TCP JPEG 수신, 디코딩 및 RGB565 Line/Tile Buffer 출력
7. 5 FPS 달성 여부 판단 후 LCD 시연 또는 PC Viewer Plan B 확정

### 4.4 의사결정이 필요한 항목

- 최종 MQTT Topic, Payload Schema, QoS 및 Retain 정책
- STM32 이후 MQTT 전송 경로를 PC 브리지로 유지할지 여부
- 대상 AP의 BSSID와 고정 채널 운영 가능 여부
- 노드 위치 좌표계와 3DGS 좌표계 변환 규칙
- 백엔드 Time Window 크기와 Timestamp 기준
- 스마트 IMU 모델 및 구매 상태
- LCD 핀맵, PSRAM 사용 전략 및 Plan B 판단

---

## 5. 구성원별 진척도

아래 내용은 현재 임베디드 작업 저장소와 임베디드 파트에서 확인 가능한 연동 항목을 기준으로 작성하였다. 네트워크/백엔드 및 그래픽스 파트의 전체 진척률은 각 담당자의 별도 확인이 필요하다.

| 구성원 | 담당 영역 | 보고 시점 진척 내용 | 다음 협업 항목 |
|---|---|---|---|
| 이나영 | 임베디드 | ESP32 RSSI Node/Gateway, ESP-NOW 패킷, UART 프로토콜, STM32 수신·전처리·JSON 생성, Serial-MQTT Bridge 구현. 관련 빌드와 파서 시험 통과 및 서버 연결 정상 확인 | 3개 이상 실물 동시 시험, 현재 MQTT 규격 문서화, 장시간·장애 시험, IMU·LCD 착수 |
| 강주호 | 네트워크/백엔드 | MQTT Broker 및 서버 연결이 정상 동작하며 임베디드 데이터 Publish 경로가 확보됨 | Topic/Schema/QoS 문서화, Time Window 동기화, Dashboard 바인딩 및 성능 검증 |
| 이승우 | 그래픽스 | 임베디드 측에서 노드 좌표를 결합할 수 있는 `node_positions.json` 구조를 마련했으나, 3DGS 좌표계의 실제 좌표 및 변환 규칙은 미확정 | 좌표축·단위·원점 합의, 노드 위치 반영, 향후 UDP Pose·Position Snap 인터페이스 정의 |
| 공통 | 통합 및 문서화 | 임베디드 프로토콜 문서와 시험 계획 작성, 기본 데이터 흐름 및 서버 연결 확인 | 공통 Schema 동결, Dashboard·Graphics 통합 로그 확보, 성능 지표와 시연 절차 정리 |

---

## 6. 보고 시점까지의 과제 수행 내용 및 중간 결과

### 6.1 수행 내용

#### 가. ESP32 RSSI 노드

- FreeRTOS 기반으로 RSSI 측정, ESP-NOW 송신, Health Log 기능을 별도 Task로 분리하였다.
- 대상 AP RSSI를 200 ms 간격으로 측정하고, 최근 5개 유효값의 이동평균을 계산하도록 구현하였다.
- 1초 주기로 Node ID, Sequence, Uptime, Raw RSSI, Filtered RSSI, Sample Count 및 Error Flags를 전송한다.
- 각 노드의 송신 시작 시간을 Node ID별로 분산하여 동시 송신 충돌 가능성을 낮추었다.
- ESP-NOW 송신 성공·실패 횟수와 Low Heap 상태를 확인할 수 있도록 하였다.

#### 나. ESP32 게이트웨이

- 원격 노드의 ESP-NOW 패킷을 수신하고 Magic, Version, Length, CRC32를 검증하도록 구현하였다.
- Node ID별 최신 Sequence와 수신 수, 중복 수, 유실 수를 관리한다.
- 게이트웨이 보드도 로컬 RSSI 노드 5로 동작하도록 구성하였다.
- 수신값을 `$RSSI,...*Checksum` UART Line으로 변환하여 STM32로 전송한다.
- 실제 노드가 부족할 때 사용할 수 있는 Fake RSSI 모드를 마련했으며, 현재 기본 설정에서는 비활성화되어 있다.

#### 다. STM32 수신 및 전처리

- STM32F107VCT6의 USART1을 115200 bps로 설정하였다.
- RX 인터럽트에서 Byte 단위로 데이터를 수신하고 완성된 UART Line을 파싱한다.
- 정상, Checksum 오류, Format 오류를 각각 집계한다.
- 노드별 최신 RSSI, Sequence, Age 및 Error Flags를 관리한다.
- 수신 데이터가 갱신되면 MQTT-ready JSON Snapshot을 생성하여 UART로 출력한다.
- CMake/Ninja 기반 빌드와 J-Link 플래시를 완료하였고, Under-reset 저속 연결 방식에서 MCU 실행 상태를 확인하였다.

#### 라. MQTT 및 좌표 연계

- Python Serial-MQTT Bridge에서 STM32의 JSON Line을 읽어 MQTT 메시지로 변환하도록 구현하였다.
- 과거 `nodes` 배열 형식과 현재 `readings` 배열 형식을 모두 처리할 수 있도록 호환 로직을 추가하였다.
- Numeric Node ID를 `node-01`~`node-04`, `gw-01` 형식으로 정규화하였다.
- 노드 위치를 별도 JSON 설정에서 읽어 `pos_x`, `pos_y`, `pos_z`로 결합한다.
- Publish 결과를 확인하고 잘못된 RSSI 범위나 JSON을 제외하도록 구현하였다.

### 6.2 중간 시험 결과

| 시험 항목 | 결과 | 비고 |
|---|---|---|
| STM32 Debug 빌드 | 성공 | 현재 Build Tree 기준 추가 작업 없음 |
| ESP32 Node 빌드 | 성공 | Binary 약 0xB8950 bytes, App Partition 약 28% 여유 |
| ESP32 Gateway 빌드 | 성공 | Binary 약 0xBC870 bytes, App Partition 약 26% 여유 |
| STM32 Parser Host Test | 성공 | 정상 Line 파싱, 오류 Checksum 거부, Node Table 및 JSON 생성 확인 |
| Python Script 문법 검사 | 성공 | Publisher 및 Serial-MQTT Bridge 대상 |
| STM32 실물 Flash | 성공 이력 있음 | J-Link Under-reset, 1 MHz 연결에서 실행 확인 |
| ESP32 3개 이상 동시 실물 시험 | 미완료 | 보드별 설정·배포 및 로그 수집 필요 |
| MQTT Broker 및 서버 연결 | 성공 | 현재 연결 및 데이터 Publish 정상 동작 |
| Dashboard·Graphics 활용 시험 | 진행 중 | 서버 수신 데이터의 최종 표시·활용 검증 필요 |
| 1시간·2시간 연속 안정성 시험 | 미완료 | 장애 대응 구현 후 수행 예정 |
| Median Filter 비교 시험 | 미완료 | 동일 위치 원시 데이터 확보 후 분석 예정 |

STM32 파서 시험에서는 두 노드의 데이터를 포함한 JSON Snapshot이 정상 생성되었고, 잘못된 Checksum을 가진 UART Line은 거부되었다. 현재 결과는 통신 모듈의 기본 동작을 입증하지만, 실제 다중 노드 무선 환경에서의 손실률과 장시간 안정성을 입증한 결과는 아니다.

### 6.3 현재 확보한 산출물

- ESP32 RSSI Node Firmware
- ESP32 ESP-NOW/UART Gateway Firmware
- ESP-NOW Binary Packet 및 UART Line Protocol 문서
- STM32 UART Parser, Preprocessor 및 MQTT JSON Builder
- STM32F107VCT6 실행 Firmware 산출물(ELF/HEX/BIN/MAP)
- STM32 Serial-MQTT Bridge
- 노드 위치 설정 파일
- Host Parser Test 및 단계별 Test Plan
- 임베디드 파트 상세 구현 계획서

### 6.4 종합 평가

현재까지의 결과로 볼 때 **RSSI를 측정하여 STM32가 구조화된 데이터로 수신·전처리하고 서버로 전송하는 핵심 소프트웨어 경로는 확보되었다.** 또한 ESP32와 STM32의 빌드 가능 상태, 파서의 정상·오류 처리, MQTT 연계용 브리지와 서버 연결까지 확인되어 멀티 노드 계측 실험을 시작할 기반은 마련되었다.

다만 과제의 핵심 성공 기준은 서버 연결 성공에 더해, **3개 이상의 실물 노드가 안정적으로 데이터를 보내고 백엔드가 이를 시간 단위로 묶어 대시보드와 그래픽스 파트에서 사용할 수 있는 상태**이다. 따라서 다음 보고 시점까지는 현재 MQTT 규격 문서화, 3개 이상 실물 동시 시험, 필터 비교 데이터 확보 및 대시보드·그래픽스 연동을 최우선으로 진행한다.

---

## 7. 보고 근거 자료

- `2026 졸업과제 기획안(260331).pdf`
- `2026전기_착수보고서_16_이거왜됨연구회_3DGS 기반 공간 데이터 시각화.pdf`
- `임베디드_파트_구현_계획_최종본.md`
- `CODEX_ESP32_RSSI_TO_STM32_TASK.md`
- `rssi_esp32_to_stm32/README.md`
- `rssi_esp32_to_stm32/docs/protocol.md`
- `rssi_esp32_to_stm32/docs/test_plan.md`
- `rssi_esp32_to_stm32/esp32_node/`
- `rssi_esp32_to_stm32/esp32_gateway/`
- `stm32_final_term/`
- `stm32_serial_mqtt_bridge.py`
- `node_positions.json`
