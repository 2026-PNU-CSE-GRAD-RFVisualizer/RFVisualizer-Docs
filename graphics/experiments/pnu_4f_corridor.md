# PNU 4층 복도 RSSI 예비 실험 계획

## 0. 한 줄 결론

**TX 1대와 고정 보정 RX 4대, 이동 Test RX 1대를 모두 높이 0.45 m에 놓고, 장치 편차를 60초 측정한 뒤 6개 Test 지점을 각각 30초 측정하여 후속 Sionna RT·IDW 비교에 사용할 RSSI 값을 확보한다.**

이 계획은 최종 논문 통계 실험보다 **파이프라인 연결과 예비값 확보**를 우선한다. Test 지점이 6개뿐이므로 결과의 통계적 신뢰도에는 한계가 있다.

---

## 1. 실험 목표와 완료 기준

### 목표

- PNU 4층 복도에서 위치가 기록된 실제 RSSI를 확보한다.
- ESP32 5대 사이의 수신 감도 차이를 장치별 offset으로 보정한다.
- 고정 보정점 4개와 평가 전용 Test 지점 6개를 명확히 분리한다.
- 이후 `Raw Sionna RT`, `Plain IDW`, `Sionna RT + Residual IDW` 비교에 사용할 입력값을 만든다.

### 완료 기준

다음 항목을 모두 만족하면 현장 측정을 완료한 것으로 판단한다.

- [ ] 실제 MQTT 패킷에서 RSSI 필드 의미와 배율을 확인했다.
- [ ] TX 1개, Cal 4개, Test 6개의 좌표가 모두 등록됐다.
- [ ] ESP32 5대의 장치 offset이 모두 계산됐다.
- [ ] 각 Test 지점에 유효 샘플이 목표 25개 이상 저장됐다.
- [ ] `calibration_points.csv`에 4개 지점이 있다.
- [ ] `test_points.csv`에 6개 지점이 있다.
- [ ] 여러 BSSID 혼합, TX 좌표 누락, 장치 offset 누락 문제가 없다.
- [ ] 원본 JSONL·SQLite와 Export 폴더를 별도 위치에 백업했다.

### 이번에 선택한 범위

| 방식 | 시간 | 신뢰도 | 이번 적용 |
|---|---:|---:|---|
| Test 6개를 1회씩 측정 | 짧음 | 예비 결과 수준 | **적용** |
| Test 6개를 2~3회 반복 | 중간 | 시간 변화 확인 가능 | 후속 권장 |
| Test 15개 이상 측정 | 김 | 논문 평가에 더 적합 | 이번에는 제외 |

---

## 2. 공간 좌표계

이번 실험은 기존 `classroom_20260723` 강의실 좌표가 아니라 **`pnu_4f_corridor` 복도 좌표**를 사용한다.

- 원점: 도면 왼쪽 아래의 녹색 점
- `+X`: 도면에서 오른쪽
- `+Y`: 도면에서 위쪽
- `+Z`: 바닥에서 위쪽
- 단위: m
- 공통 높이: `z = 0.45 m`

`z = 0.45 m`는 의자 위 배치 높이로 통일한다. 현장에서는 의자 위치와 장치 방향이 바뀌지 않도록 테이프로 표시한다.

> 주의: 기존 `configs/rf_experiment/classroom_20260723/` 설정은 깊이 범위와 지점 수가 이번 복도 실험과 다르므로 사용하지 않는다. 후속 처리에서는 별도의 `pnu_4f_corridor` Scene·TX/RX 설정을 만든다.

---

## 3. 확정 좌표

표에서는 공통값인 `z = 0.45 m`를 생략했다. 백엔드 좌표 등록 CSV에는 `z=0.45`를 반드시 포함한다.

### 3.1 TX

| ID | 역할 | X | Y | 비고 |
|---|---|---:|---:|---|
| `tx-01` | AP/TX | 3.15 | 0.50 | 실험 중 위치·방향·전원 설정 고정 |

### 3.2 고정 보정 RX

| ID | X | Y | 배치 원칙 |
|---|---:|---:|---|
| `cal-01` | 3.13 | 3.93 | 실험 종료까지 고정 |
| `cal-02` | 8.08 | 3.93 | 실험 종료까지 고정 |
| `cal-03` | 10.56 | 10.68 | 실험 종료까지 고정 |
| `cal-04` | 8.69 | 15.63 | 실험 종료까지 고정 |

### 3.3 이동 Test RX

| 측정 순서 | ID | X | Y |
|---:|---|---:|---:|
| 1 | `test-01` | 0.65 | 3.93 |
| 2 | `test-02` | 5.60 | 3.93 |
| 3 | `test-03` | 10.56 | 3.93 |
| 4 | `test-04` | 6.92 | 11.20 |
| 5 | `test-05` | 6.92 | 15.63 |
| 6 | `test-06` | 10.56 | 15.63 |

### 3.4 장치 편차 측정 위치

- ID: `offset-00`
- 대표 좌표: Cal-01과 같은 `(3.13, 3.93, 0.45) m`
- 배치: Cal-01 주변에서 ESP32 5대를 최대한 가깝게 놓는다.
- 조건: 높이, 방향, TX와의 시야 조건을 동일하게 맞춘다.
- 용도: 장치 간 편차 계산에만 사용하며 공간 보정점이나 Test 지점으로 사용하지 않는다.

---

## 4. 장치 역할

실제 `node_id`는 `sniff.py`에 표시된 문자열을 그대로 사용한다. 현장에서 임의로 이름을 바꾸지 않는다.

| 역할 | 실제 `node_id` | 배치 위치 |
|---|---|---|
| 고정 보정 센서 1 | 현장 기록: __________ | `cal-01` |
| 고정 보정 센서 2 | 현장 기록: __________ | `cal-02` |
| 고정 보정 센서 3 | 현장 기록: __________ | `cal-03` |
| 고정 보정 센서 4 | 현장 기록: __________ | `cal-04` |
| 이동 Test 센서 | 현장 기록: __________ | `test-01`부터 `test-06`까지 이동 |

권장 배치는 백엔드의 기본 리허설과 같이 `node1→cal-01`, `node2→cal-02`, `node3→cal-03`, `node5→cal-04`, `node4→Test 이동`이다. 실제 펌웨어 ID가 다르면 위 표에 대응 관계를 기록한다.

---

## 5. 실험 조건

### 고정해야 하는 조건

- 동일한 AP BSSID와 Wi-Fi 채널을 전체 실험 동안 사용한다.
- 가능하면 2.4 GHz 채널을 수동 고정하고 자동 채널 변경을 끈다.
- TX 위치, 높이, 방향, 출력 설정을 바꾸지 않는다.
- 모든 RX는 같은 높이와 같은 장치 방향을 유지한다.
- 고정 Cal RX 4대는 배치 후 실험 종료까지 움직이지 않는다.
- 측정 중 사람은 TX와 RX 사이에 서 있지 않는다.
- 의자 중심과 장치 방향을 테이프로 표시한다.

### 반드시 기록할 환경 상태

- 실험 날짜와 시작·종료 시각
- AP SSID, BSSID, 채널, 주파수
- 백엔드 Git 커밋과 펌웨어 버전
- 405·406·407 출입문 개폐 상태
- 엘리베이터 문과 운행 여부
- 복도 통행 인원 또는 큰 물체 이동
- 재측정한 지점과 재측정 이유

엘리베이터 운행, 문 개폐, 다수 인원 통과가 측정 중 발생하면 메모하고 해당 세션을 다시 측정하는 것을 우선한다.

---

## 6. 준비물

- [ ] 고정 Cal용 ESP32 4대
- [ ] 이동 Test용 ESP32 1대
- [ ] 전용 AP/TX와 전원 장치
- [ ] Gateway·STM32·노트북과 필요한 케이블
- [ ] 의자 또는 높이 0.45 m를 유지할 받침
- [ ] 줄자 또는 레이저 거리 측정기
- [ ] 위치·방향 표시용 마스킹 테이프와 펜
- [ ] 현장 도면과 이 체크리스트
- [ ] 사진 기록 장치
- [ ] 결과 백업용 USB 또는 별도 저장 위치

---

## 7. 실험 전 점검

### 7.1 백엔드 설정

논문 실험에서는 다음 설정을 유지한다.

```env
ENABLE_REALTIME=false
DEFAULT_SESSION_SECONDS=30
EXPECTED_SAMPLES_PER_POINT=30
```

실제 RSSI 패킷을 먼저 확인한다.

```powershell
python sniff.py
```

확인 항목:

- `node_id`가 5대 모두 서로 다른가?
- `timestamp`가 밀리초 단위로 정상 증가하는가?
- `ap_bssid`와 `ap_channel`이 고정 AP와 일치하는가?
- `rssi`가 필터링 RSSI인가?
- 일반적인 값이 `-55` 전후인가, `-550` 전후인가?

`-553`처럼 10배 정수로 전송되면 다음과 같이 설정한다.

```env
RSSI_FILTERED_SCALE=10
```

이미 dBm 단위인 `-55.3` 또는 `-55`로 전송되면 `1.0`을 사용한다.

### 7.2 압축 리허설

```powershell
python rehearsal.py --seconds 2
```

다음을 모두 확인한 뒤 본 실험을 시작한다.

- [ ] MQTT가 연결됐다.
- [ ] ESP32 5대가 모두 online이다.
- [ ] 5대 모두 RSSI가 수신된다.
- [ ] 세션 자동 종료와 Export가 동작한다.
- [ ] 비정상 RSSI 또는 잘못된 배율이 없다.

### 7.3 현장 표시

1. 원점과 `+X`, `+Y` 방향을 도면과 대조한다.
2. TX, Cal 4개, Test 6개 위치의 의자 중심을 테이프로 표시한다.
3. 모든 지점의 높이가 0.45 m인지 확인한다.
4. 장치 앞 방향을 테이프 화살표로 표시한다.
5. 전체 배치와 원점이 함께 보이도록 사진을 찍는다.

---

## 8. 본 실험 절차

### 8.1 실험 시작

1. 백엔드와 MQTT 브로커를 실행한다.
2. `experiment_id`를 다음 규칙으로 생성한다.

   ```text
   pnu_4f_corridor_YYYYMMDD_HHMMSS
   ```

3. AP BSSID와 채널을 입력하고 실험을 시작한다.
4. TX 좌표 `(3.15, 0.50, 0.45)`와 주파수를 등록한다.
5. 노드 5대가 모두 online인지 다시 확인한다.

### 8.2 장치 offset 측정: 60초

1. ESP32 5대를 `offset-00`에 모은다.
2. 장치 사이 간격과 방향을 가능한 한 동일하게 맞춘다.
3. UI에서 **전체를 `offset-00`으로** 배치한다.
4. 역할을 `offset`, 측정 시간을 `60`초로 설정한다.
5. 측정 중 사람과 물체를 움직이지 않는다.
6. 종료 후 5대 모두 목표 50개 이상의 유효 샘플이 있는지 확인한다.
7. 장치 offset을 즉시 계산한다.
8. 노드 누락, 표준편차 5 dB 초과, 샘플 수 불균형이 있으면 60초 전체를 다시 측정한다.

### 8.3 고정 Cal RX 배치

1. 고정 RX 4대를 `cal-01`부터 `cal-04`까지 배치한다.
2. UI에서 실제 `node_id`와 Cal 지점의 대응을 등록한다.
3. 이동 RX를 제외한 4대는 이후 절대 움직이지 않는다.
4. 모든 장치 방향과 높이를 확인하고 배치 사진을 찍는다.
5. 10초 이상 기다린 후 30초 안정화 세션을 한 번 실행한다.

고정 Cal RX는 이후 6개의 Test 세션 동안에도 각자의 위치에서 데이터를 계속 누적한다. 따라서 Cal은 약 210초, 각 Test는 30초 데이터가 저장된다.

### 8.4 Test 6개 측정

각 지점에서 다음 절차를 반복한다.

1. 이동 RX를 지정된 테이프 중심에 놓는다.
2. 높이 0.45 m와 방향을 확인한다.
3. 장치를 놓은 뒤 10초 이상 기다린다.
4. UI의 `point_id`와 실제 위치가 일치하는지 소리 내어 교차 확인한다.
5. 역할을 `test`, 이동 센서 `node_id`, 측정 시간을 `30`초로 설정한다.
6. 측정을 시작하고 사람은 전파 경로 밖으로 이동한다.
7. 유효 샘플 수와 MQTT 연결을 확인한다.
8. 이상이 없으면 다음 Test 지점으로 이동한다.

| 순서 | point_id | 좌표 `(x, y)` | 완료 | 재측정 사유 |
|---:|---|---|---|---|
| 1 | `test-01` | `(0.65, 3.93)` | [ ] | |
| 2 | `test-02` | `(5.60, 3.93)` | [ ] | |
| 3 | `test-03` | `(10.56, 3.93)` | [ ] | |
| 4 | `test-04` | `(6.92, 11.20)` | [ ] | |
| 5 | `test-05` | `(6.92, 15.63)` | [ ] | |
| 6 | `test-06` | `(10.56, 15.63)` | [ ] | |

### 8.5 즉시 재측정할 조건

다음 중 하나라도 발생하면 해당 세션을 버리고 같은 지점을 다시 측정한다.

- MQTT 연결이 끊겼다.
- 이동 RX의 유효 샘플이 18개 미만이다.
- 목표 샘플 수 25개에 크게 못 미친다.
- 다른 BSSID 또는 채널의 데이터가 섞였다.
- 장치가 움직이거나 방향이 바뀌었다.
- 사람이 TX와 RX 사이를 오래 가로막았다.
- 엘리베이터 운행이나 문 개폐가 결과에 영향을 줬다고 판단된다.
- RSSI 표준편차가 5 dB를 초과하고 원인이 일시적 환경 변화로 보인다.

---

## 9. 좌표 등록용 CSV

다음 내용을 백엔드의 좌표 등록 입력에 사용한다.

```csv
point_id,point_role,pos_x,pos_y,pos_z,note
offset-00,offset,3.13,3.93,0.45,cal-01 주변 장치 편차 측정점
cal-01,calibration,3.13,3.93,0.45,고정 보정 RX 1
cal-02,calibration,8.08,3.93,0.45,고정 보정 RX 2
cal-03,calibration,10.56,10.68,0.45,고정 보정 RX 3
cal-04,calibration,8.69,15.63,0.45,고정 보정 RX 4
test-01,test,0.65,3.93,0.45,이동 평가 RX 1
test-02,test,5.60,3.93,0.45,이동 평가 RX 2
test-03,test,10.56,3.93,0.45,이동 평가 RX 3
test-04,test,6.92,11.20,0.45,이동 평가 RX 4
test-05,test,6.92,15.63,0.45,이동 평가 RX 5
test-06,test,10.56,15.63,0.45,이동 평가 RX 6
```

좌표 등록 후 장치 offset을 다시 계산하고 Export를 실행한다.

---

## 10. 현장 종료 전 품질 검사

### 필수 산출물

```text
experiments/<experiment_id>/
├── raw/measurements_raw.csv
├── processed/measurements_summary.csv
├── processed/calibration_points.csv
├── processed/test_points.csv
├── config/points.csv
├── config/tx_rx.json
├── config/device_offsets.json
├── config/sessions.json
├── qc_report.json
└── README.md
```

추가로 다음 원본을 반드시 보존한다.

- `data/experiment.db`
- `data/ingest_raw.jsonl`

### 기대 집계

| 항목 | 기대값 |
|---|---:|
| 장치 offset | 5대 |
| Calibration 지점 | 4개 |
| Test 지점 | 6개 |
| Summary 그룹 | 15개: offset 5 + Cal 4 + Test 6 |
| Test 지점당 유효 샘플 | 목표 25~30개, 최소 18개 |

현재 네트워크 백엔드의 품질 검사는 Test 15개를 기준으로 작성되어 있다. 따라서 이번 실험에서는 다음 경고가 나오는 것이 정상이다.

```text
Test 위치가 15개가 아닙니다 (현재 6개)
```

이 경고만 존재하는 것은 허용한다. 다음 문제는 허용하지 않는다.

- `TX(AP) 좌표가 등록되지 않았습니다.`
- `offset 값이 없는 노드`
- `좌표 미등록 위치`
- `여러 BSSID가 섞였습니다.`
- `샘플 수 부족`

### RFVisualizer CSV 계약 검사

Export 폴더를 RFVisualizer에 복사한 뒤 실행한다.

```bash
python -m tools.rf_experiment.main validate-csv \
  --kind raw \
  --csv experiments/<experiment_id>/raw/measurements_raw.csv \
  --require-rows

python -m tools.rf_experiment.main validate-csv \
  --kind summary \
  --csv experiments/<experiment_id>/processed/measurements_summary.csv \
  --require-rows
```

두 명령이 모두 exit code 0이어야 한다.

> 백엔드가 내보내는 `config/tx_rx.json`은 현재 RFVisualizer Marker 계약과 형식이 다르다. RSSI CSV 확보에는 문제가 없지만, Sionna RT 실행 전에는 이번 좌표를 RFVisualizer 형식의 새 `pnu_4f_corridor` Marker 설정으로 옮겨야 한다.

---

## 11. 백업과 현장 기록

실험 종료 직후 다음 순서로 보존한다.

1. Export 폴더 전체를 복사한다.
2. `experiment.db`와 `ingest_raw.jsonl`을 복사한다.
3. 현장 도면과 배치 사진을 같은 폴더에 넣는다.
4. 백엔드 커밋, 펌웨어 버전, `.env` 설정을 텍스트로 기록한다.
5. 원본은 수정하지 않고 분석용 복사본을 따로 만든다.

권장 백업 구조:

```text
backup/pnu_4f_corridor_<날짜>/
├── backend_export/
├── raw_database/
├── field_photos/
├── floor_plan/
└── environment_notes.md
```

---

## 12. 예상 소요 시간

| 단계 | 예상 시간 |
|---|---:|
| 장비 연결·패킷 확인·리허설 | 15분 |
| 위치 표시와 배치 사진 | 15분 |
| 장치 offset 측정·검사 | 5분 |
| Cal 배치·안정화 | 10분 |
| Test 6개 이동 측정 | 10~15분 |
| Export·품질 검사·백업 | 10분 |
| 재측정 여유 | 20분 |
| 합계 | 약 65~80분 |

---

## 13. 결과 해석 시 제한사항

- Test 지점이 6개이므로 이번 결과는 예비 실험으로 표현한다.
- Test 데이터는 IDW fitting이나 파라미터 선택에 사용하지 않는다.
- IDW와 Residual IDW fitting에는 Cal 4개만 사용한다.
- 고정 Cal은 여러 Test 세션 동안 누적되고 각 Test는 30초만 측정되므로 관측 시간이 서로 다르다.
- 모든 높이를 0.45 m로 통일한 결과이므로 다른 높이의 전파 분포를 대표하지 않는다.
- 엘리베이터, 문, 사람 이동의 영향을 완전히 제거할 수 없으므로 환경 기록과 함께 해석한다.
- 이번 좌표와 RSSI는 `pnu_4f_corridor` 장면에만 사용하며 기존 강의실 실험과 섞지 않는다.

---

## 14. 현장에서 기억할 것

**장치 방향과 높이를 고정하고, `point_id`를 매번 확인하며, 문제가 생긴 세션은 현장을 떠나기 전에 반드시 다시 측정한다.**
